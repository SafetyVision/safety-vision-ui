import { useState } from 'react';
import {
  Form,
  FormGroup,
  Input,
  Label,
  Button,
  Toast,
  ToastHeader,
  ToastBody,
} from 'reactstrap';
import axios from 'util/axiosConfig';
import { Link, Navigate } from 'react-router-dom';

export default function DeviceManagerPage() {
    return (
        <p>Device Manager Page</p>
    );
}